# Multi-Tenant Isolation

Every engagement requires a unique namespace, separate artifact storage, separate retrieval scope, separate credentials, separate telemetry tags and an explicit access policy.

Never use a single unfiltered vector index across all clients.

Only sanitised and generalised assets may be promoted to shared patterns.
